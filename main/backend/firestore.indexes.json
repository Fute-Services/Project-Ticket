{
  "_comment": "Composite indexes needed by the paginated list endpoints. Deploy with: firebase deploy --only firestore:indexes. Until they exist the API falls back to an in-memory sort (see utils/complaints.js), which works but reads the whole collection.",
  "indexes": [
    {
      "collectionGroup": "hr_complaints",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "user_id", "order": "ASCENDING" },
        { "fieldPath": "submitted_at", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "it_complaints",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "user_id", "order": "ASCENDING" },
        { "fieldPath": "submitted_at", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
